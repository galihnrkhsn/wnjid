# Folder ini isinya file .sql (skema/migrasi) yang dibaca manual saat development,
# bukan diakses lewat web. Blokir total.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
