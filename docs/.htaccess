# Folder ini isinya catatan dokumentasi internal (.md), tidak untuk publik. Blokir total.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
